{
    "__default__" :
    {
        "account" : "bioinfo",
        "cpus-per-task" : 12,
        "partition" : "short",
        "job-name": "{rule}_{wildcards}",
        "output": "/scratch/baotram/test/results/log/{rule}_{wildcards}.out",
        "error": "/scratch/baotram/test/results/log/{rule}_{wildcards}.err",
        "mail-user": "",
        "mail-type": "ALL",
    },

    "guppy_basecalling" :
    {
        "partition": "gpu",
        "account": "gpu_group",
        "cpus-per-task": {CPU_THREADS_PER_CALLER},
    },

    "guppy_demultiplexing" :
    {
        "partition": "gpu",
        "account": "gpu_group",
        "cpus-per-task": {CPU_THREADS_PER_CALLER},
    }
}
